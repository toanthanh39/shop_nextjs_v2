import { SplashIcon } from "../icons";

export default function SplashLoading() {
	return (
		<main className="fixed inset-0 z-modal w-screen h-screen flex items-center justify-center bg-white">
			<SplashIcon></SplashIcon>
		</main>
	);
}
